# Essential environment variables for Docker
DJANGO_SETTINGS_MODULE=pyerp.settings.development
DEBUG=True
SECRET_KEY=dev_secret_key_replace_in_production

# PostgreSQL Database Configuration (external database)
# These values will be overridden by environment variables if set
DB_ENGINE=django.db.backends.postgresql
DB_NAME=pyerp_testing
DB_USER=postgres
DB_PASSWORD=UZ3*cMrCuFJm-fTA7csE
DB_HOST=192.168.73.65
DB_PORT=5432

ALLOWED_HOSTS=localhost,127.0.0.1

# Redis configuration (local to container)
REDIS_URL=redis://localhost:6379/0

# Celery configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Email settings (development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=25

# Static and media files
STATIC_URL=/static/
MEDIA_URL=/media/
STATIC_ROOT=/app/static
MEDIA_ROOT=/app/media

# Development-specific settings
PYTHONPATH=/app
DJANGO_DEBUG=True

# CSRF and Session Cookie Settings
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
CSRF_TRUSTED_ORIGINS=http://localhost:8050,http://127.0.0.1:8050,https://192.168.73.65 